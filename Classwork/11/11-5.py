from ADT import DoublyLinkedList

lnk_list = DoublyLinkedList.DoublyLinkedList()
lnk_list.add_last(2)
lnk_list.add_last(4)
lnk_list.add_last(6)

print(lnk_list)

def remove_all(lnk_list, val):
    cursor = lnk_list.first_node()
    while(...):
        pass
